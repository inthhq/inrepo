---
"inrepo": minor
---

Make package.json dependency linking explicit and opt-in with per-package `packageJson` config and new `add` flags. Source vendoring no longer changes dependency buckets by default, and legacy `dev` config now returns migration guidance.
