---
"inrepo": minor
---

Support packages rooted in monorepo subdirectories, including npm `repository.directory` discovery, explicit `--repository-directory` sources, shared repository snapshots, lockfile v3 provenance, and dependency-graph replay using published workspace dependency rewrites.
