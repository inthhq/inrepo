---
"inrepo": minor
---

Retain integrity-pinned npm artifacts for registry dependencies and fill publish-only runtime files that are absent from immutable git checkouts. Artifact-assisted locks use version 5 and replay offline from a content-addressed cache without replacing repository source.
